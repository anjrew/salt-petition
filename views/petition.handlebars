<section>
    {{>log-out}}

    {{>slogan}}

    {{>logo}}

    {{#if error}} {{>error_message}} {{/if}}

    <form method="post">

        {{>signature}} 

        {{>submit-button}}
        
    </form>

</section>
 
