<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <link href="https://fonts.googleapis.com/css?family=Permanent+Marker&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Montserrat&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <title>Andrew Johnson</title>
</head>
<video id="video" autoplay loop>
         <source src="/video.mp4" type="video/mp4">
</video>
<body>
     
    <div id="overlay"></div>

    <div id="popup" class="popup">
        <form method="post" id="popup-right">
            <button type="submit" value="cookiesAccepted" id="close-popup">X</button>
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
        </form>
        <h3>Welcome to LAVA LAMPS UNITED!</h3>
        <p>This site uses cookies and if you're using it we have to assume that you are ok with that.</p>
    </div>

    

    {{#if loggedIn}}
        {{>nav-bar}}
    {{else}}
         <div class="balance"></div>
    {{/if}}

    {{>slogan}}
    <div class="row" id="pages">
        {{!-- Model popup    --}}

        <section class="page" id="delete-popup">
            {{>delete-profile}}
        </section>

        {{!-- Main render --}}
        <section class="page" id="main">
            <div class="module">{{{ body }}}</div>
        </section>

        <section class="page" id="about">
            {{>about}}
        </section>
    </div>
    
    {{#unless error}}
        
    {{/unless}}
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>
        <script src="/script.js"></script>
        <div class="balance"></div>
    </body>
</html>